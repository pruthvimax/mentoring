# Smart Mentoring — Backend (Node.js + Express + MongoDB)

## Setup

1. Copy files into a folder `smart-mentoring-backend`.
2. Create `.env` based on `.env.example`.
3. Install dependencies:
   ```bash
   npm install
